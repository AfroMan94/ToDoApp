@model IEnumerable<ToDoApp.Models.ToDo>

@*<div id="progressBar">
	<div class="progress">
		<div class="
		@if (@ViewBag.Percent == 100)
		{
			@MvcHtmlString.Create("progress-bar-success");
		} else {
			@MvcHtmlString.Create("progress-bar");
		}
		" role="progressbar" aria-valuenow="@ViewBag.Percent" aria-valuemin="0" 
			 aria-valuemax="100" >@(ViewBag.Percent)%</div>
	</div>
</div>*@


	<table class="table table-striped">


		@foreach (var item in Model)
		{
			<tr>
				<td id="@item.Id" class="itemDescription">
					@if (!item.isDone)
					{
						@Html.DisplayFor(modelItem => item.Description)
					}
					else
					{
						<div class="completedToDo">
							@Html.DisplayFor(modelItem => item.Description)
						</div>
					}

				</td>
				<td>
					@Html.CheckBoxFor(modelItem => item.isDone, new { id = item.Id, @class = "ActiveCheck" })
				</td>
				<td>
					@Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-primary" })
					@Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-info" })
					
					<button id="@item.Id" class="btn btn-danger delete-row">Delete</button>
			</td>
			</tr>
		}

	</table>

<script src="~/Scripts/Custom_Scripts/GenerateProgressBar.js"></script>
<script src="~/Scripts/Custom_Scripts/CkeckBoxListener.js"></script>
<script src="~/Scripts/Custom_Scripts/Delete.js"></script>

@*<script src="~/Scripts/Custom_Scripts/EditDescription.js"></script>*@